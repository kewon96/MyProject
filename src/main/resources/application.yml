
server:
  port: 3030

## database 접속 설정
spring:
  datasource:
    url: jdbc:postgresql://localhost:8010/springdata
    username: kewon
    password: pass

  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non-contextual-creation: true

        ddl-auto: update

    database-platform: org.hibernate.dialect.PostgreSQLDialect